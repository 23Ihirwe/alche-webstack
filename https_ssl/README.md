# HTTPS SSL

This project covers HTTPS, SSL termination, DNS configuration, and HTTP to HTTPS redirection using HAProxy.

## Tasks

- Configure DNS subdomains for the web servers and load balancer.
- Configure HAProxy SSL termination.
- Redirect HTTP traffic to HTTPS.
